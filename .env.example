# Base domain on the certificate
LE_BASE_DOMAIN=hjalti.me

# Bind nginx only on your LAN interface (prevents WAN exposure)
# Set to your Docker host LAN IP (example below); CI defaults to 127.0.0.1
LAN_BIND=192.168.144.10

# Timezone for logs
TZ=Atlantic/Reykjavik
