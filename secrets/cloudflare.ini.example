# Copy to secrets/cloudflare.ini (do NOT commit the real file)
# chmod 600 secrets/cloudflare.ini
# Create a Cloudflare API token with Zone.DNS:Edit on the target zone(s)
dns_cloudflare_api_token = YOUR_CLOUDFLARE_DNS_TOKEN

# Tip: scope the token to ONLY the hjalti.me zone, DNS:Edit.
